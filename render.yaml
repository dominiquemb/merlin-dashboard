services:
  - type: static_site
    name: merlin-dashboard-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    # Render static sites automatically handle _redirects file for client-side routing




