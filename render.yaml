services:
  - type: static_site
    name: merlin-dashboard-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    # Note: Client-side routing rewrite must be configured in Render Dashboard:
    # Go to Settings > Redirects and Rewrites
    # Add rule: Source: /*, Destination: /index.html, Action: Rewrite




